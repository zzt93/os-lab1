language: c
compiler:
- gcc
os:
- linux
sudo: true

before_install:
- sudo apt-get -qq update
- sudo apt-get install -y python3
- python3 --version
- sudo pip install enum34

script:
- make hardDisk

